/*package org.openxava.scm.calculators;

import org.openxava.calculators.*;
import org.openxava.scm.model.*;

import static org.openxava.jpa.XPersistence.*;  //For using getManager()

public class PricePerUnitCalculator implements ICalculator{
	
	*//**
	 * 
	 *//*
	private static final long serialVersionUID = 1L;
	private String number; // Contains the part number when calculate() is called
	
	public Object calculate() throws Exception {
		Part part = getManager() // getManager() from XPersistence
		.find(Part.class, number).find(SupplierOrderDetail.class, part.getNumber()); // Find the product
		return part.; // Returns its price
	}
	
	public void setNumber(String number) {
		this.number = number;
	}
	public String getNumber() {
		return number;
	}
}*/
